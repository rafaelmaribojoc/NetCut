# NetCut Backend Dependencies (Termux-Friendly)
# No Pydantic - avoids Rust compilation issues
# Install with: pip install -r requirements.txt

starlette>=0.32.0
uvicorn>=0.24.0
scapy>=2.5.0
apscheduler>=3.10.0
